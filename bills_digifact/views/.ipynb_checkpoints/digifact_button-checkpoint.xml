<?xml version="1.0" encoding="utf-8"?>

<odoo>
    <record id="account_move_inherit" model="ir.ui.view">
        <field name="name">account.move.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_invoice_sent']" position="after">
                <button name="validar_factura_electronica_facturacion" string="Validar Factura Electronica" type="object" attrs="{'invisible':['|', '|', ('state', '!=', 'posted'), ('is_move_sent', '=', True), ('move_type', 'not in', ('out_invoice', 'out_refund'))]}" class="btn-primary"/>
            </xpath>
        </field>
    </record>
</odoo>